import {Platform} from 'ionic-angular';
import {Injectable} from '@angular/core';

@Injectable()
export class FbProvider {platform: Platform;
    constructor( platform: Platform ) {
    }

    login() {
     
    }
}

